//Botão curtidas
document.addEventListener("DOMcontentLoaed" , () =>{
    const lik8tn = ocument. queySelector (".left.action .action.btn:firs.chil");
    if (!lik8yn) return;
    const like = lik8tn. querySelector9("syg");

    //localiza o contador

    let textNode = Array. from(like8th.childNodes) .find(node) => node.nodeType

    === Node. TEXT_NODE && node. textContent.trin() !== ""
) ;

let cont = 0;

if(textNode){

    textNode. textContent = `0` ;
}

function applyLikeSyle(){
    likeSvg.style.fill = "red";
    likeSvg.style.stroke = "red"
    likeSvg.style.color = red

likeSvg.style.transform = "scale(1.3)";
setTimeout(() => (likeSvg.style.transform = "scale(1)" {
    
}, timeout);


})

